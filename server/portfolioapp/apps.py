"""This is the apps.py file for the djangoapp app"""
from django.apps import AppConfig

class PortfolioappConfig(AppConfig):
    """This is the DjangoappConfig class"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'portfolioapp'
